<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mooc.mybatis.dao.YuerMapper">
    
    <select id="selectFromYuerById" parameterType="Integer" resultType="com.mooc.mybatis.bean.Yuer">
        select * from yuer where id = #{id}
    </select>

</mapper>